<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_po_demand_tree" model="ir.ui.view">
        <field name="name">po.demand.tree</field>
        <field name="model">po.demand</field>
        <field name="arch" type="xml">
            <list string="Purchase On Demand">
                <field name="name"/>
                <field name="state" widget="badge" decoration-info="state == 'draft'" decoration-success="state == 'confirm'"/>
            </list>
        </field>
    </record>

    <record id="view_po_demand_form" model="ir.ui.view">
        <field name="name">po.demand.form</field>
        <field name="model">po.demand</field>
        <field name="arch" type="xml">
            <form string="Purchase On Demand">
                <header>
                    <button name="action_confirm" string="Confirm" type="object" class="btn-primary" invisible="state != 'draft'" groups="purchase.group_purchase_user"/>
                    <button name="action_cancel" string="Cancel" type="object" invisible="state != 'draft'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,confirm"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_purchase_orders" type="object" class="oe_stat_button" icon="fa-credit-card" invisible="purchase_count == 0" groups="purchase.group_purchase_user">
                            <field name="purchase_count" widget="statinfo" string="Purchase Orders"/>
                        </button>
                    </div>

                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Reference..." readonly="state != 'draft'"/>
                        </h1>
                    </div>


                    <notebook>
                        <page string="Product Lines">
                            <field name="line_ids" readonly="state != 'draft'">
                                <list editable="bottom">
                                    <field name="product_id"/>
                                    <field name="vendor_id"/>
                                    <field name="qty"/>
                                    <field name="price"/>
                                </list>

                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_po_demand" model="ir.actions.act_window">
        <field name="name">Purchase On Demand</field>
        <field name="res_model">po.demand</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_po_demand"
              name="Purchase On Demand"
              parent="purchase.menu_procurement_management"
              action="action_po_demand"
              sequence="20"/>
</odoo>
